import moment from "moment"

export default class ThingsController {

    async index(req, res, next) {
        try {
            
            
        } catch (error) {
            next(error)
        }
    }
}